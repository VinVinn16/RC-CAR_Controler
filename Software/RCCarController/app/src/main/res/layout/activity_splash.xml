<?xml version="1.0" encoding="utf-8"?>
<!-- Layout Splash Screen: Latar hitam dengan nama aplikasi di tengah -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/splash_bg"
    android:padding="32dp">

    <!-- Ikon/Logo placeholder (ganti dengan icon RC Car jika tersedia) -->
    <TextView
        android:id="@+id/tv_splash_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🚗"
        android:textSize="72sp"
        android:layout_marginBottom="24dp"/>

    <!-- Nama Aplikasi -->
    <TextView
        android:id="@+id/tv_splash_app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="36sp"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:letterSpacing="0.05"
        android:layout_marginBottom="8dp"/>

    <!-- Subtitle -->
    <TextView
        android:id="@+id/tv_splash_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Arduino Bluetooth Controller"
        android:textSize="14sp"
        android:textColor="#AAAAAA"
        android:letterSpacing="0.1"/>

</LinearLayout>
